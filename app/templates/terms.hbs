<!-- Terms and Conditions Page -->
<div class="container">
    <div class="row">
        <div class="col-md-12 text-left">
            {{#link-to 'index' tagName="div"}}<h4><a href="">Back to Home</a></h4>{{/link-to}}
        </div>
    </div>
</div>

{{ log model }}

<section id="team" class="bg-light-gray">
    <div class="container">
        <div class="row">
            <div class="col-lg-12 text-center">
                <h2 class="section-heading">STANDARD TERMS AND CONDITIONS</h2>
                <hr>
                {{#each model as |term|}}
                <h3 class="section-subheading text-muted">{{term.no}}. {{term.subject}}</h3>
                <p>{{term.condition}}</p>
                <br>
                {{/each}}
            </div>
        </div>
    </div>
</section>

{{agency-footer}}

{{outlet}}
